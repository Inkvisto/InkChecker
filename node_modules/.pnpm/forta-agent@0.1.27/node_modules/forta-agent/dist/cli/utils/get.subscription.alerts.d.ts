import { Alert, BotSubscription, GetAlerts } from "../../sdk";
export declare type GetSubscriptionAlerts = (subscriptions: BotSubscription[], createdSince: Date) => Promise<Alert[]>;
export declare function provideGetSubscriptionAlerts(getAlerts: GetAlerts): GetSubscriptionAlerts;
